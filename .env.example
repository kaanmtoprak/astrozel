GEONAMES_USERNAME=

# Development only — comma-separated LAN hosts allowed to load Next.js /_next/* .
# Loopback (127.0.0.1, localhost) is always allowed in next.config.
# Example: ASTROZEL_ALLOWED_DEV_ORIGINS=192.168.1.100
# Do not commit real personal network IPs.
ASTROZEL_ALLOWED_DEV_ORIGINS=
